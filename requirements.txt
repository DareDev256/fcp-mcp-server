# MCP Server Framework
# Floor is 1.3.0: server.py imports
# `mcp.server.lowlevel.helper_types.ReadResourceContents`, which was introduced
# in mcp 1.3.0. Older versions ImportError at startup and the client shows a
# bare "disconnected". That type carries the text/html mime type for the
# preview:// resource, so there is no fallback path.
# Ceiling is 2.0.0: mcp 2.0 removed the low-level Server decorator API.
# Keep in sync with the same pin in pyproject.toml.
mcp>=1.3.0,<2.0.0

# XML Parsing
lxml>=4.9.0

# Dev / Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
